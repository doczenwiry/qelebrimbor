Overview
========

Installation
------------

The tool can be installed by cloning the repository to a suitable location.

.. code-block:: bash

    git clone https://github.com/doczenwiry/qelebrimbor
    cd qelebrimbor

``qb`` currently operates on a limited dataset of randomly generated ZX-graphs from PyZX.
In order to generate the dataset after cloning, you can issue the following command:

.. code-block:: bash

    python benchmarking/generate-dataset-rings-small.py


Pipeline
--------


``qb`` works by